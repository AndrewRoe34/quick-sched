Need to work out ideas for a proper database design


1. Need to work out the themes

Option A:
    - User
    - Task
    - Card
    - Label
    - CheckList
    - Board

Option B:
    -User
    -JBin


1NF

ID      FName      LName      Password      Email            JBin      ScriptLog       SystemLog
0       Andrew     Roe        ABCD123       aproe@ncsu.edu   ...       ...             ...


2NF


Login Info
Email --> Password

Account
Email --> (FName, LName, JbinID, ScriptLogId, SystemLogId)

Jbin Data
JbinID --> JbinData

Script Log Data
ScriptLogId --> ScriptLog

System Log Data
SystemLogId --> SystemLog


Script Samples:

#static function
db: retrieve_db_user("email", "password")

str: db.retrieve_script_log(script_log_id)
print(str)

#can also provide a * to represent all
str: db.retrieve_syslog(system_log_id)
print(str)

Uses only a max of 40 char when displaying the log (would do the same for all attributes)


Sample output for listing all the data contents that only the user should be able to access:

ScriptLogID | ScriptLog                                                 | Date
0           | SCRIPT_LOG:\nPREPROC_ATTR: DEF_CONFIG=false, IMPORT=t...  |  10/13/2023
1           | SCRIPT_LOG:\nPREPROC_ATTR: DEF_CONFIG=true, IMPORT=fa...  |  10/15/2023
2           | SCRIPT_LOG:\nPREPROC_ATTR: DEF_CONFIG=false, IMPORT=f...  |  10/16/2023

If the user wishes to see the entire ScriptLog, they can specify:
str: db.retrieve_scrlog(0)


str: db.retrieve_custom("select scriptlogid, scriptlog, email, pw, date from ...")



Key objectives:
- Will need to show work of 1NF, 2NF, and 3NF
- Will need to provide an ERD diagram
- Will need to integrate the core software with the system
- Will need to integrate with the scripting language (least of our worries)

Question: How much extra credit is this worth?





















