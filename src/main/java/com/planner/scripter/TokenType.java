package com.planner.scripter;

public enum TokenType {
    COMMENT,
    SPACE,
    NEW_LINE,
    IDENTIFIER,
    BUILT_IN_OBJECT,
    BUILT_IN_FUNC,
    VAR,
    IF,
    ELIF,
    ELSE,
    TRUE,
    FALSE,
    INCLUDE,
    INCLUDE_FLAG,
    FUNC,
    STRING_LITERAL,
    INTEGER_LITERAL,
    LEFT_PAREN,
    RIGHT_PAREN,
    COMMA,
    DOT,
    COLON,
    EQUAL_EQUAL,
    EOF
}
