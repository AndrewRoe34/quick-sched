package agile.planner.scripter;

public class FunctionState extends State {
    @Override
    protected void processFunc(String line) {
        //create local type variables here (i.e. what's stored/passed via the function call)

    }
}
