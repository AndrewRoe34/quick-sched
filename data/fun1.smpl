#Sample script
include: __CURR_CONFIG__, __DEBUG__, __LOG__, __IMPORT__, __EXPORT__, __BUILD__, __STATS__

func foo(x)
  #test
  println(x)

my_label: label("MA", 3)
foo(my_label)

###TODO: Need to finish adding functionality to allow storage of variables for arguments to functions while maintaining the same reference###