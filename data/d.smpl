include: __CURR_CONFIG__, __LOG__, __BUILD__

balance: 0
func deposit(x)
  println("Current savings: ", balance.++(x))

func loop()
  println("Welcome to Bank of America!")
  deposit(100)

loop()


#ToDo
    #Support calling functions inside the if condition for argument
    #fix the above logic bug
    #allow variable declaration inside custom functions
    #add more static functions to system